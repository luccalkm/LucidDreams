import { Typography } from '@mui/material'

export const Home = () => {

    return (
        <Typography variant="h3">
            Meus sonhos
            {[1,2,3,4].map({registered}) => {}}
        </Typography>
    )
    
}